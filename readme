里面使用了swing编写，netbeans画的界面，注册热键使用jintellitype
任务调度，表达式cron
界面主题SwingSets3
pom打jar包直接运行，
打过之后的jar包需要删除META-INF文件夹下的JAVAHELP.DSA，EVA_EPC_.SF，EVA_EPC_.RSA
不删除无法运行

里面使用了动态生成java文件，动态编译运行
使用了反射
读写json，excel文件

文件查重功能：
   1、首先选择要排除的文件夹（可以手动填写，也可以选择，用分号隔开）
   2、在选择哪个盘符，或者是全部的盘符，或者是某个文件夹（记录）
   3、选择查询文件大小的限制（包括功能大文价查询）
   4、按条件检索，先检索出来所有符合文件，每次删除之后需要手动检索文件（要获取之前第二步的条件）
        检索文件按照时间倒序
        保留检索的话，如果结果是改变的话 那么之前检索的内容也要发生改变
   5、按照文件的大小或者是文件名相同分别搜索文件（记录）
   6、删除文件，多选（只保留一个）
        删除按照日期新旧 ， 结果顺序
        删除的时候还要获取之前第五步的搜索条件
检索和删除的时候 未完成之前按钮不可用，完成之后按钮还原
   后期加上进度条
   
 
 版本修改：  
   7、查询文件（可以使用通配符）
   8、查询文本内容
   保留搜索改为 建立索引（在文件排除和查询范围中产生）
   list列表改为表格形式，给表格加上鼠标菜单
   索引利用日志功能存储到本地 或者存储为json文件
   
文件的重命名功能：
   1、
   
   
   
   

<html><body>*文件名：通过文件名模糊查询路径<br>*文件类型：可以使用通配符* 如*.txt多个可以用英文,(逗号隔开)<br>*文本内容：先查询文件路径是否包含，再查询文本文件是否包含内容<br>
文本文件类型有：txt,doc,docx,ppt,pptx,xls,xlsx,pdf<br>*正则表达式：根据正则表达式查询</body></html>    
<html><body>格式里面1和2组合使用<br>格式1里面的数字是1,2，3。。。阿拉伯数字<br>汉字是一二三。。。<br>格式2中的前缀和后缀是对于格式1来说的<br>
位置里面的前后缀是对于格式1和格式2组合来说的<br>覆盖：如果只选择了格式2自定义重命名，结果为：“内容”_1,也就是内容+数字顺序组合</body></html>     